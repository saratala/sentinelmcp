# Prometheus scrape config for SentinelMCP.
# Started via: docker-compose --profile observability up -d
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: sentinelmcp
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8888"]
